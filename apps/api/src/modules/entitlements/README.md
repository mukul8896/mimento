# entitlements (deferred)

This bounded module is reserved and intentionally empty in Phase 1. It is scheduled for Phase 2
(see `docs/phase-status.md`). Do not add a placeholder implementation; the provider port lives
in `src/providers/` where applicable.
